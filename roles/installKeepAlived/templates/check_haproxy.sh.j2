#!/bin/bash

if [ `curl https://{{ ansible_ssh_host }}:{{ k8s_ha_master_haproxy_port }} --insecure |grep kind |wc -l` -eq 0 ] ; then
   exit 1 # just exit, MASTER will reduce weight(-25), so vip will move on BACKUP node
fi
